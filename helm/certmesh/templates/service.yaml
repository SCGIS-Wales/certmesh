apiVersion: v1
kind: Service
metadata:
  name: {{ include "certmesh.fullname" . }}
  labels:
    {{- include "certmesh.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "certmesh.selectorLabels" . | nindent 4 }}
